apiVersion: v1
kind: Service
metadata:
  name: translator-svc
  namespace: translator
  labels:
    app: translator
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: translator